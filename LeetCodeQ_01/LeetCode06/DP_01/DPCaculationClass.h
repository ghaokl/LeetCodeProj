#pragma once
#include <vector>
#include <algorithm>

using namespace std;
class DPCaculationClass
{
public:
	int videoStitching(vector<vector<int>>& clips, int T);
	int longestMountain(vector<int>& A);
	vector<int> smallerNumbersThanCurrent(vector<int>& nums);
};

